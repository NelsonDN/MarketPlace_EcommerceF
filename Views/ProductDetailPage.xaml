<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MarketFaith.Views.ProductDetailPage"
             BackgroundColor="White"
             Title="Détails produit">
    <StackLayout>
        <Label Text="{Binding name}" HorizontalOptions="Center" TextColor="Black" FontSize="24" Margin="10" />
        <Image Source="{Binding avatar}" HeightRequest="200" WidthRequest="200" Aspect="AspectFill" />
        <Label Text="{Binding prix, StringFormat='Prix : {0:C}'}" TextColor="Red" HorizontalOptions="Center" FontSize="20" Margin="10" />
        <Label Text="{Binding pesage}" TextColor="#4e69a6" HorizontalOptions="Center" FontSize="20" Margin="10" />
        <Label Text="{Binding description}" TextColor="Black" HorizontalOptions="Center" FontSize="20" Margin="10" />
    </StackLayout>
</ContentPage>